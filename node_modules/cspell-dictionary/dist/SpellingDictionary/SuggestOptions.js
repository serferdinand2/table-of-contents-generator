export function createSuggestOptions(numSuggestions, compoundMethod, numChanges, ignoreCase) {
    return {
        numSuggestions,
        compoundMethod,
        numChanges,
        ignoreCase,
    };
}
